/*#pragma once
#include "Engine/GameObject.h"

namespace CR {
	class CollisionEvent {
	public:
		CollisionEvent(const GameObject& sender, const GameObject& target)
			: sender(sender), target(target) {}

		const GameObject& sender, target;
	};
}*/